export enum EnvironmentType {
  DEV = 'development',
  TEST = 'test',
  PRODUCTION = 'production'
}

export enum EnvironmentDefault {
  PORT = '4000',
  SECRET = 'usercrud',
  TOKEN_EXPIRATION = '3600'
}
